-- 코드를 입력하세요
SELECT  (T1.PRICE DIV 10000) * 10000    AS 'PRICE_GROUP'
    ,   COUNT(*)                        AS 'PRODUCTS'
FROM    PRODUCT T1
GROUP BY
        T1.PRICE DIV 10000
ORDER BY
        T1.PRICE DIV 10000  ASC